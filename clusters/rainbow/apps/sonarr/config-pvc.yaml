---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sonarr-config-pvc
  namespace: vpn
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: pink-nfs
  volumeMode: Filesystem
  resources:
    requests:
      storage: 1Gi