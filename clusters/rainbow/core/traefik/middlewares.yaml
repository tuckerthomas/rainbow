apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: hsts-headers
  namespace: kube-system
spec:
  headers:
    stsSeconds: 31536000           # 1 Year duration
    stsIncludeSubdomains: true
    stsPreload: true
    forceSTSHeader: true
